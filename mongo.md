> use metadatatemplates
switched to db metadatatemplate
> db.createUser({user: "metadatatemplates", pwd: "met001",roles: [ "readWrite", "dbAdmin" ]})
Successfully added user: { "user" : "metadatatemplates", "roles" : [ "readWrite", "dbAdmin" ] }
> exit
bye
